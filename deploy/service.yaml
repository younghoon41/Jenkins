apiVersion: v1
kind: Service
metadata:
  name: sk052-devops-svc
  namespace: class-2
spec:
  selector:
    app: sk052-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
